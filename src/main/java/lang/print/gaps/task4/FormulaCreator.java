package lang.print.gaps.task4;

public class FormulaCreator {
    public static void main(String[] args) {
        //I don't really get the task, so I will just write the formula into a variable
        String formula = "(9 * a * a - 5 * b + 2 + a - 7) * ((a + b - 4 * a * b) / 2)";
    }
}
